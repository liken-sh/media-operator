# The nested case of what display-operator's weston.go writes. The [core]
# keys are its keys; the output is the wayland backend's one window instead
# of a connector on a card.

[core]
shell=ivi-shell.so
modules=liken-layout.so
renderer=gl
require-input=false
idle-time=0
